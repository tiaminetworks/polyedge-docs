source "https://rubygems.org"

# Use GitHub Pages gem to match the GitHub Pages environment
gem "github-pages", group: :jekyll_plugins

# Optional plugins
gem "jekyll-sitemap" # Generates a sitemap.xml
gem "jekyll-feed"    # Generates an RSS feed
